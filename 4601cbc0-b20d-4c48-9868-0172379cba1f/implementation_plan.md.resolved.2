# Notification Permission Fix & APK Build

## User Review Required
> [!IMPORTANT]
> I will be modifying [src/utils.js](file:///c:/Users/admin/.gemini/%EC%8A%A4%EC%9C%84%EC%B9%98%EC%9B%B9%EC%84%9C%EB%B9%84%EC%8A%A4/daily-habit-tracker/src/utils.js) to explicitly request notification permissions. This is required for Android 13+.

## Proposed Changes
### Frontend Logic
#### [MODIFY] [utils.js](file:///c:/Users/admin/.gemini/스위치웹서비스/daily-habit-tracker/src/utils.js)
- Add `LocalNotifications.checkPermissions()` and `LocalNotifications.requestPermissions()` before scheduling.
- Handle permission denial gracefully.

#### [MODIFY] [Stats.jsx](file:///c:/Users/admin/.gemini/스위치웹서비스/daily-habit-tracker/src/Stats.jsx)
- Update "Total Checks" calculation to count only checks for the *current day* instead of all-time history.
- Remove spacer div used for fixed navigation.

#### [MODIFY] [Profile.jsx](file:///c:/Users/admin/.gemini/스위치웹서비스/daily-habit-tracker/src/Profile.jsx)
- Remove spacer div used for fixed navigation.

### UI / Styling
#### [MODIFY] [App.css](file:///c:/Users/admin/.gemini/스위치웹서비스/daily-habit-tracker/src/App.css)
- Update `.bottom-nav` class to remove `position: fixed`.
- Style Navigation as a static card that appears at the end of the content flow (matching `.glass-card` style).

### Android Build Config
- [MODIFY] [variables.gradle](file:///c:/Users/admin/.gemini/스위치웹서비스/daily-habit-tracker/android/variables.gradle)
    - Update `targetSdkVersion` from 32 to 33 to fully support Android 13+ notification permissions.

## Verification Plan
### Automated Tests
- None available for this specific interaction.

### Manual Verification
1.  **Build APK**: Run `npm run mobile:build` and then build APK via Gradle.
2.  **Install & Test**: User will need to install the APK on a device/emulator.
    - Go to Profile -> Toggle Notifications.
    - Verify that the system permission dialog appears (if not previously denied).
    - Verify that "Daily reminder set" toast appears on success.
