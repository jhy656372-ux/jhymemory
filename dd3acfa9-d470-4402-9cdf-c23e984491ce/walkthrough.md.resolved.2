# UI Refinements

I have refined the UI based on your feedback.

## Changes

### 1. Speech Bubble Position
- **Moved Down**: Adjusted the speech bubble position (`top: -10px`) so it sits lower and doesn't get cut off by the top of the screen.

### 2. New Habit Button
- **Icon Update**: Changed the "New Habit" button icon from a Plus (`➕`) to an Arrow (`→`) to match the style of the navigation icons below.

## Verification
1.  **Speech Bubble**: Check the **Dashboard** and verify the "Let's do this!" bubble is fully visible and not cut off.
2.  **Add Button**: Verify the button next to the "New habit..." input now shows an arrow icon.
