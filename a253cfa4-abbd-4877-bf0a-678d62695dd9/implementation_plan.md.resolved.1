# Implementation Plan - Bible Text Cleanup & Verification

## Goal Description
The user wants to remove `&#x27;` and `!` characters from the Bible text.
The user also requested a Memo feature, which appears to be already implemented in the codebase. I will verify its functionality and ensure it meets all requirements (persistence, pagination, UI).

## Proposed Changes

### Bible Text Cleanup
#### [MODIFY] [BiblePage.jsx](file:///c:/Users/admin/.gemini/나의 사랑하는 책/src/pages/BiblePage.jsx)
- Update the text replacement logic to remove `!` and `&#x27;`.
```javascript
// Change regex from /&#x27;/g to /(&#x27;|!)/g or chain replaces
```

### Memo Feature
- **Verification Only**: The [MemoPage.jsx](file:///c:/Users/admin/.gemini/%EB%82%98%EC%9D%98%20%EC%82%AC%EB%9E%91%ED%95%98%EB%8A%94%20%EC%B1%85/src/pages/MemoPage.jsx) and [HomePage.jsx](file:///c:/Users/admin/.gemini/%EB%82%98%EC%9D%98%20%EC%82%AC%EB%9E%91%ED%95%98%EB%8A%94%20%EC%B1%85/src/pages/HomePage.jsx) already contain the requested code. I will verify that the "Memo" button exists under "Prayer" and the page functions as requested (5 items/page, local storage).

## Verification Plan
### Automated/Manual Verification
- **Bible Page**: Check text content to ensure `!` and `&#x27;` are removed.
- **Memo Page**: Verify adding > 5 items triggers pagination, and toggles work.
