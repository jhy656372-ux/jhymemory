# Final Adjustments Plan

## Admin Page
### Gallery Section
- Change the fixed grid from **12 slots** to **8 slots**.
- `const slots = Array(8).fill("");`

## Home Page (Gallery Section)
- Confirm `.slice(0, 4)` logic is present (already verified).

## Footer
### Vertical Separators
- **Remove** the separator divs entirely for a clean look.

## Data
- Keep `photos: []` empty (User will upload manually).
- Increment version to 20.
